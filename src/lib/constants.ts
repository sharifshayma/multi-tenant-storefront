export const DEFAULT_PRICE_MINOR = 4000;
// Platform default brand name (a fallback shown until a store sets its own
// branding) — kept as a fixed Arabic/English pair rather than moved into the
// dictionary. Used by src/lib/messages.ts to localize the {siteName}
// placeholder in outgoing order-status messages.
export const SITE_NAME = "متجري";
export const SITE_NAME_EN = "My Store";
